<%= t('mailers.friendship.facebook_friend_sign_up.hello',
      name: user_name_for(@user),
      locale: (@user.language || I18n.locale))
%>
<%= t('mailers.friendship.facebook_friend_sign_up.follow',
      friend: user_name_for(@friend),
      locale: (@user.language || I18n.locale))
%>
===============================================

<%= t('mailers.friendship.facebook_friend_sign_up.visit_profile_text',
      link: url_for(:host => t('mailers.url', locale: (@user.language || I18n.locale)),
                    :controller => "users",
                    :action => "show",
                    :id => @friend.username),
      friend: user_name_for(@friend),
      locale: (@user.language || I18n.locale))
%>

