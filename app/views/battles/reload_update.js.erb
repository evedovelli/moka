$('#edit_battle_<%= @battle[:id] %>').replaceWith("<%= j (render 'form') %>");

$('.upload_picture').hide();
$('.delete_picture').hide();
$('.picture_preview').hide();

jQuery(previewImageClick);
jQuery(deletePhoto);

jQuery(validateNewBattle);

jQuery(function() {
  $('.remove_form').click(function() {
    removeForm($(this).closest('.battle_form'));
  });
});

jQuery.extend(jQuery.validator.messages, {
  required: "<%= t('messages.required') %>"
});

