$('#battle<%= @battle[:id] %>').after("<%= j (render 'form') %>");
$('#battle<%= @battle[:id] %>').hide();

$('.upload_picture').hide();
$('.delete_picture').hide();
$('.picture_preview').hide();

jQuery(previewImageClick);
jQuery(deletePhoto);

jQuery(validateNewBattle);

jQuery(function() {
  $('.remove_form').click(function() {
    removeForm($(this).closest('.battle_form'));
  });
});

jQuery.extend(jQuery.validator.messages, {
  required: "<%= t('messages.required') %>"
});

jQuery(setOptionSizes($('#edit_battle_<%= @battle[:id] %>')));

jQuery(setupDurationInput($('#edit_battle_<%= @battle[:id] %>')));
