$('#battle_form').html("<%= j (render 'form') %>");
$('#battle_form').slideDown(200);

$('.upload_picture').hide();
$('.delete_picture').hide();
$('.picture_preview').hide();

$('#add_battle').slideUp(200);

jQuery(previewImageClick);
jQuery(deletePhoto);

jQuery(validateNewBattle);

jQuery(function() {
  $('.remove_form').click(function() {
    removeForm($(this).closest('.battle_form'));
    $('#add_battle').show();
  });
});

jQuery.extend(jQuery.validator.messages, {
  required: "<%= t('messages.required') %>",
  min: "<%= t('messages.greater_than_zero') %>"
});

jQuery(checkboxButton);
