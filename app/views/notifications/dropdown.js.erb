$("#notifications-btn").html("<%= escape_javascript(notifications_btn(current_user)) %>");
$("#notification-dropdown").html("<%= escape_javascript(render @notifications) %>");
jQuery(showsUnfollowButton);
