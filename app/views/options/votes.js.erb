$('<%="#vote_index#{@option.id}"%>').replaceWith("<%= j (render :partial => 'votes/index',
                                                                :locals => {
                                                                  :option => @option
                                                                }) %>");
