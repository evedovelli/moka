<%= simple_form_for(@contest, remote: true, :html => {:class => 'form-horizontal' }) do |f| %>
  <div class="main-box">
    <h2 class="center"><%= t 'contests._form.title' %></h3>

    <%= f.error_notification %>

    <%= f.association :stuffs, as: :check_boxes %>
    <%= f.input(:starts_at, :as => :datetime_picker, :class => "datetimepicker") %>
    <%= f.input(:finishes_at, :as => :datetime_picker, :class => "datetimepicker") %>

    <div class="row">
      <div class="span12">
        <div class="center">
          <%= f.button(:submit, @contest.new_record? ? t('buttons.create') : t('buttons.update'), :class => "btn btn-primary") %>
          <%= link_to(t('buttons.cancel'), :back, :onclick => "$('#contest_form').slideUp(350)", :class => "btn") %>
        </div>
      </div>
    </div>
  </div>
<% end %>


