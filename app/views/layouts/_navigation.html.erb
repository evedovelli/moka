<div class="navbar-inner">
  <div class="container">
    <%= link_to t('layouts._navigation.page_title'), root_path, :id => "logo" %>
    <% if user_signed_in? %>
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
    <% end %>

    <div class="menu">
      <% if user_signed_in? %>
        <nav class="nav-collapse">
          <ul class="nav pull-right">
            <li><%= link_to t('layouts._navigation.home'), user_home_path %></li>
            <li><%= link_to t('layouts._navigation.logout'), destroy_user_session_path, :method=>'delete' %></li>
            <li><%= link_to t('layouts._navigation.edit_account'), edit_user_registration_path %></li>
          </ul>
        </nav>
      <% end %>
    </div>
  </div>
</div>

