<% @battle.options.each do |option| %>
  $('<%="#results#{option.id}"%>').replaceWith("<%= j (render :partial => 'votes/results', :locals => { :id => option.id, :votes => option.number_of_votes }) %>");
<% end %>
